to get started simply run pip install -r requirements.txt to install all of the depedencies
and when running press CTRL + f1 to activate voice recognition this will run until it decets that the speach has stoped it will then prosses this and give you the responce